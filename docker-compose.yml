services:
  quota-dashboard:
    build: .
    image: quota-dashboard:latest
    container_name: quota-dashboard
    ports:
      - "8765:8765"
    volumes:
      # Monta tu config.json real (con tus claves API) sobre el template.
      # Si no existe, se usa config.example.json del interior de la imagen.
      - ./config.json:/app/config.json:ro
      # Persiste las sesiones (cookies) entre reinicios del contenedor.
      - ./sessions:/app/sessions
      # Persiste las sesiones de ChatGPT (refresh tokens) entre reinicios.
      - ./chatgpt_sessions:/app/chatgpt_sessions
      # Persiste las sesiones de z.ai (platform tokens) entre reinicios.
      - ./zai_sessions:/app/zai_sessions
    restart: unless-stopped
